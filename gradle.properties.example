# Copy to gradle.properties (local) or pass -P in CI. Do not commit real secrets.

# Backend reachable from emulator (host machine) or your VPS public URL
BACKEND_BASE_URL=http://10.0.2.2:8000/

# YouTube Data API v3 key (free quota)
YOUTUBE_API_KEY=
